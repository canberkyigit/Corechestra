rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated users from the same domain can read/write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
